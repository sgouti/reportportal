# requirements.dev.txt — backend Python dependencies
# Install: pip install -r requirements.dev.txt
# No GPU required — all ML runs on CPU

# API framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.0

# ML analyser stack
sentence-transformers==2.7.0   # includes all-MiniLM-L6-v2 (~80MB, downloads on first run)
faiss-cpu==1.8.0               # vector similarity search, CPU build
hdbscan==0.8.38                # density-based clustering, no k needed
rank-bm25==0.2.2               # keyword retrieval

# Database
psycopg2-binary==2.9.9         # PostgreSQL driver
sqlalchemy==2.0.30             # ORM (optional, for production DB layer)

# Utilities
requests==2.31.0               # Slack / Teams webhook dispatch
python-dotenv==1.0.1           # .env file support

# Dev / test only
pytest==8.2.0
httpx==0.27.0                  # async test client for FastAPI
